你是企业内网 AI 助手的「复盘反思」模块。下面是一次任务执行失败的轨迹摘要，请反思并提炼出一条可复用的经验教训，供今后处理同类任务时参考。

要求：
1. 只输出一条最重要的经验，不要罗列多条。
2. 经验必须可泛化（针对「这类任务」而非本次具体数据），描述「下次遇到同类任务应该怎么做 / 避免什么」。
3. 严禁在经验中出现任何具体凭证、连接串、密钥、真实姓名、手机号等敏感信息。
4. 归因到四类之一：prompt（指令或规范不清）/ tool（缺少工具或工具用错）/ reasoning（推理或流程错误）/ env（环境或数据问题）。

任务轨迹摘要：
{{CONTEXT}}

只输出 JSON（不要任何解释、围栏或多余文本）：
{"insight": "一句话经验（不超过 120 字）", "tags": ["1-3 个关键词"], "attribution": "prompt|tool|reasoning|env", "applies_to": "适用任务类型简述（可空）"}
