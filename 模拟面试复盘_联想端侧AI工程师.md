---

## 0. 全文导航与主线速览（考前 5 分钟看这节；开场前背 0.4 自我介绍）

**章节导航：**


| 章节                                      | 内容                                          | 用途         |
| ----------------------------------------- | --------------------------------------------- | ------------ |
| [第 1 章](#第-1-章技术深挖题按子系统分组) | 技术深挖 10 题（架构/编排/鲁棒性/检索与推理） | 面试主体     |
| [第 2 章](#第-2-章产品场景与行为面试)     | 产品设计 2 题 + 行为 2 题 + 反问环节          | 下半场       |
| [第 3 章](#第-3-章高概率问题速查表)       | 未深挖领域的应答锚点表                        | 防偷袭       |
| [第 4 章](#第-4-章临场弹药)               | 答题纪律 + 数字脚本 + 杀手锏故事线            | 直接背       |
| [第 5 章](#第-5-章面试前-24-小时检查清单) | 一致性自查/材料/话术/当天执行                 | 考前逐项打勾 |

**技术深挖 17 题全景索引（按面试切入角度快速定位）：**


| 域        | 题号 | 主题           | 一句话核心答案                                                 |
| --------- | ---- | -------------- | -------------------------------------------------------------- |
| 架构      | Q1   | 分工与通信     | Rust 管端、Python 管脑；HTTP + SSE 两通道，事件三处同步        |
| 架构      | Q1.5 | 技术栈选型     | 每层用约束下最合适的语言；代价用三条纪律对冲                   |
| 架构      | Q1.7 | FastAPI 服务层 | 薄网关：只校验契约、转换协议、留痕；业务判断进路由层算事故     |
| 编排      | Q2   | 双框架路由     | 三级级联：关键词→LLM→先验；路由只是策略先验不是硬分叉        |
| 编排      | Q2.8 | 意图识别       | 五级管线：快路径优先、结构化输出、永不阻塞                     |
| 编排      | Q2.5 | LangGraph 选型 | 给 AI 用的工作流引擎（BPM 背景）；确定性编排 + 策略层分离      |
| 编排      | Q2.6 | 编排范式       | Coding 靠验证所以要计划，Work 靠反应所以用 ReAct；差异在策略层 |
| 编排      | Q2.7 | 多 Agent 调度  | 把中台调度工程搬过来；最小必要上下文 + 死信 + 限流是生存条件   |
| 鲁棒      | Q3   | 解析容错       | 收窄输出空间而不是堆 Grammar；不盲目重试，降级到确定性结果     |
| 鲁棒      | Q3.5 | HITL 移交 UX   | 认知负担摊到全过程；结构化摘要 + 让用户选不让用户想            |
| 鲁棒      | Q3.7 | 验证体系       | 测试金字塔逐层隔离不确定性；让"跑不了"发生在上线前             |
| 鲁棒      | Q3.8 | 评测集         | 概率系统的单元测试；门槛写进 CI，坏案例滚动入库                |
| 检索/推理 | Q4   | MACC + 防幻觉  | 压缩只压表述不压事实；精确优先，引用即约束                     |
| 检索/推理 | Q4.5 | 记忆系统       | 三层记忆 + 蒸馏链；什么不该记比记什么更重要                    |
| 检索/推理 | Q4.6 | 自进化         | 不动权重的知识闭环；模型是租来的，知识是自己的                 |
| 检索/推理 | Q5   | 推测解码       | 同家族小草稿 + 四道闸防负优化；加速是可选项，正确性不是        |

**两大高频组合追问链（面试官常顺着打）：**

- **编排链**：Q2 路由 → Q2.5 框架 → Q2.6 范式 → Q2.7 多 Agent（主线：确定性优先）；
- **质量链**：Q3 容错 → Q3.7 验证 → Q3.8 评测 → Q4.6 进化（主线：一切概率行为都有确定性外壳）。

### 0.1 三句签名（贯穿全场的个人标签）

1. **技术签名**："模型可以不可靠，系统必须可靠。"（硬兜底体系）
2. **工程签名**："我习惯把指标做成可验证的门槛，而不是汇报用的亮点。"（数字口径）
3. **转型签名**："我不是放弃了过去，是带着过去的武器去了新战场。"（BPM/企业级 → AI）

### 0.2 三枚踩坑金句（技术面最打动人的永远是"我真踩过"）

- HITL 确认时 LIMIT 双重注入——回填须用原始 SQL，不能用后端改写版；
- React 组件 remount 后 useRef 状态丢失——迁 store 会话级持久化；
- SSE 慢任务静默断连 + CancelledError 穿透——心跳重连 + 取消异常单独捕获 + 终态保证。

### 0.3 一条主线（所有回答的暗线）

> **企业级工作流的确定性纪律 × AI 的概率性本质**——BPM 工作流经验让我把 LangGraph 看成"给 AI 用的工作流引擎"；七年企业级系统的校验/超时/兜底纪律，被用来驯服不可靠的小模型。技术面每个回答都应落回这条主线。

### 0.4 自我介绍脚本（开场定调，背熟）

**结构原则**：AI 项目放最前（避免"老 Java 人 AI 玩票"印象）→ 后端背景作为资产而非历史 → 落点扣岗位。每段结尾留钩子，把追问引向主场。

**2 分钟版（正式开场）：**

> "您好，我是沈凯迪。我用三段话介绍自己。
>
> 第一，过去一年半我全力在做一件事：**从 0 到 1 独立开发一个本地化 AI Agent 工作站 digPref**。它是内网环境下运行的完整产品：Tauri 2.0 桌面端 + Python 控制层 + 4 个自研 MCP Server，三语言栈。我在里面重点解决的是生产级问题：小模型下的输出稳定性硬兜底、LLM 路由的五维评估与多级降级、HITL 审批闸门、上下文压缩和推测解码加速。敏感任务强制走本地模型，全链路审计可追溯。
>
> 第二，在这之前我有 **7 年金融/政企企业级后端经验**，在信雅达从开发做到架构师和项目经理：主导过服务 50 多家银行客户的核心业务中台，拆过 6 个微服务、自研过统一报文协议；也带过 3 人团队交付银行集中作业系统。这段经历给我的是企业级的工程纪律——审批、审计、降级、可观测——后来我发现这些东西在 AI Agent 里全部重新出现了一遍。
>
> 第三，为什么投这个岗位：**端侧 + 大模型工程化恰好是我两段经历的交集**。您这边用 NPU 跑 7B 模型面对的幻觉、输出不稳定、算力受限这些问题，是我过去一年半每天都在解的题。所以今天不只是来面试，也是带着一个完整作品来交流的。谢谢。"

**30 秒版（时间紧/多人轮流介绍）：**

> "我有 7 年企业级后端背景，做过服务 50 多家银行的业务中台架构；过去一年半转型 AI，独立开发了完整的本地化 Agent 工作站，重点解决小模型下的输出稳定性、LLM 路由降级和 HITL 安全。端侧 + 大模型工程化正好是我两段经历的交集。"

**钩子设计（自我介绍里故意埋的追问入口）：**

- "输出稳定性硬兜底" → 引向杀手锏故事（见 4.3）；
- "统一报文协议" → 引向 BPM/三语言协议同步；
- "完整作品" → 引向录屏/架构图演示（见 5-C）。

**避坑提醒：**

- 不要报流水账（哪年哪月做了什么）——用"三段话"结构；
- Java 经历只占 1/3 篇幅且全部翻译成"可复用资产"；
- 学历不主动提；若被问直接用"7 年工程产出"对冲（见 5-A）。

---

## 一、JD ↔ 简历匹配度分析


| JD 要求                        | 简历对应点                                                     | 匹配度      |
| ------------------------------ | -------------------------------------------------------------- | ----------- |
| 端侧开发（Web/Windows 桌面端） | digPref：Tauri 2.0 桌面端（Rust + React/TS），Windows 打包发布 | ✅ 强匹配   |
| Python + 大模型工程化          | LangGraph 智能体工作站、LLM 路由、RAG、MCP、NL2SQL             | ✅ 强匹配   |
| 解决幻觉/输出不稳定/性能/成本  | 五维评估路由 + 熔断降级 + 缓存 + 预算控制 + 三层上下文压缩     | ✅ 强匹配   |
| 软件工程能力（测试/监控/交付） | 7 年后端 + 架构师 + 项目经理，三语言栈 CI                      | ✅ 强匹配   |
| 产品思维、发现 AI 落地场景     | 集中作业系统 PM 经历 + 自主挖掘数据专家/财税检索场景           | ✅ 匹配     |
| 加分：RPA/桌面自动化           | MCP RPA Server、OCR/影像处理经历                               | ✅ 匹配     |
| 加分：C#/WPF/WinUI             | ❌ 无，需话术化解                                              | ⚠️ 弱点   |
| LangChain/LlamaIndex           | 用 LangGraph，需说清选型理由                                   | ⚠️ 需准备 |

**防守要点：**

1. 学历若被问到，用"7 年工程产出 + 独立交付复杂系统"对冲；
2. 自我介绍把 AI 项目放最前，避免"老 Java 人、AI 玩票"印象；
3. WPF 无经验话术："端侧核心问题（进程通信/原生调用/打包分发）我在 Tauri 里完整解决过，迁移成本只在 XAML 语法层。"

---

## 第 1 章：技术深挖题（按子系统分组）

### 1.1 端侧架构

#### Q1 端侧架构：Rust（Tauri）与 Python FastAPI 的分工与通信

**分工原则：Rust 管"端"，Python 管"脑"。**

- **Rust 四职责**：① 进程生命周期（`agent_manager` 拉起 Agent 子进程，健康检查/重启，"永不阻塞窗口"降级）；② 系统级能力（OS Keyring 凭证、审计 SQLite、路径沙箱）；③ 通信代理（Webview 跨不过 CSP 直连 8765，所有请求经 Tauri command 薄代理转发）；④ 流转发（`SseBridge` 把 SSE 逐条转成 `agent://*` Tauri Event，负责取消与错误上报）。
- **Python 职责**：全部智能逻辑（意图识别、任务规划、LLM 路由、工具执行、NL2SQL、RAG），对外只暴露 HTTP + SSE。

**两条通信通道：**

- 请求-响应走 HTTP（reqwest → 127.0.0.1:8765）；
- 流式走 SSE：POST `/chat/{run_id}/stream` 启动，每个 run 一个 EventSource 消费者 + 可取消的 handle；
- 契约共享：事件名三处强制同步（`graph/stream.py` / `sse_bridge.rs` / `events.ts`），协议类型在 `shared-protocol` 包 TS/Python 双侧镜像。

**为什么混合架构（核心回答）：**

- **不纯 Python 做端侧**：桌面壳绕不开原生层；安全边界——凭证/审计不与被 LLM 驱动的进程同居；启动性能。
- **不用 Rust 重写 AI**：AI 资产是生态与迭代速度；瓶颈在 GPU 推理不在控制层。
- **本质**：把"需要安全性能的壳"与"需要快速迭代的脑"拆成两进程，HTTP/SSE 解耦；Python 层可脱离 Tauri 独立测试部署。
- **代价**：三语言栈、协议手工同步 → 用单一协议来源 + 三处同步硬纪律 + 三栈分别进 CI 对冲。

**追问要点**：SSE 不让前端直连（CSP + 代理层做取消/降级）；子进程挂了有健康检查重启 + `agent://error` 不白屏；不用 sidecar+stdio 因为 HTTP 支持独立部署与多消费者。

---

#### Q1.5 技术栈选型：为什么是 Rust + TS + Python + Tauri（Q1 递进：分工 → 选型）

**选型总原则（开场）**："每一层用它约束下最合适的语言，而不是用一种语言打天下。"桌面端有三组矛盾约束——壳要小要快、AI 生态在 Python、UI 要快速迭代——没有任何单一技术能同时满足，必然是组合，关键是**组合的边界划在哪**。

**1. 为什么 Tauri 不是 Electron？**（最没犹豫的决定，场景约束太明确：内网、政企办公机、算力受限）

- Electron 自带 Chromium+Node，安装包百 MB 级、内存高，老旧办公机体验灾难；Tauri 用系统 WebView，壳是 Rust 二进制，**安装包小一个数量级，冷启动毫秒级**；
- 政企合规要强安全模型：Tauri 的 CSP + capability 权限体系让前端默认无系统能力，每个原生能力显式授权——与"最小权限"安全观天然一致；
- 代价（主动说）：WebView 一致性差异，但目标平台是 Windows 办公环境，WebView2 是系统组件，代价可控。

**2. 为什么壳用 Rust？**
壳层干三件事：进程管理、系统级安全（Keyring 凭证/审计 SQLite/路径沙箱）、流转发。要求是**高性能、零 GC 停顿、内存安全**——桌面应用一次 GC 卡顿就是用户感知的界面冻结；Rust 类型系统把安全边界做实；交叉编译单文件二进制，**内网分发只需一个安装包无运行时依赖**——不能随便装环境的政企机器是硬需求。

**3. 为什么 UI 用 TS + React？**
UI 层约束是迭代速度和生态：Monaco 编辑器、复杂表格、图表、虚拟化列表全是现成轮子。选 TS 的具体理由：协议类型放共享包 **TS⇄Python 双侧镜像**，事件名字段类型编译期对齐——三语言系统里，类型系统是唯一免费的协议校验器。

**4. 为什么 AI 层用 Python + FastAPI？**
AI 生态重心在 Python（LangGraph/模型驱动/数据库驱动），换语言等于自研一遍；FastAPI 异步原生与 SSE 流式/并发工具调用匹配；且 Python 层**只暴露 HTTP + SSE、不依赖 Tauri**——可脱离桌面端独立测试部署，测整个 Agent 层不需要起 GUI。

**5. 收尾：代价与对冲（资深感）**

> "四个技术栈的代价我不回避：三套工具链、三套 CI、协议要人工同步。对冲是三条纪律：**协议放共享包做单一事实来源、事件名三处强制同步并有专门测试、每层都能独立测试**。这些是一次性组织成本，换来的是每层用最合适的技术、任何一层出问题不拖垮全局——长期看是赚的。"

**追问要点：**

- 为什么不用纯 Rust 写 UI（egui）？做不了富文本编辑器/复杂表格这种重交互组件，自研等于重写前端生态；WebView+TS 是站在成熟生态肩膀上；
- Python GIL/性能不担心？AI 层瓶颈在模型推理和 I/O 不在 Python 计算；重活（文档解析）用原生库；
- 一个人为什么敢用三语言？背景覆盖（7 年后端工程纪律 + 三语言都亲手写）；且三语言换来每层可独立测试——单兵作战时"隔离故障面"比"少学一门语言"更重要；
- 重做会改选型吗？大方向不变；小调整说一个真实的（协议同步早期吃过亏，后来才补上共享包+同步测试）——"重做会更快，因为知道坑在哪"。

---

#### Q1.7 FastAPI 服务层设计（架构域收尾：分工 → 选型 → 服务层）

**开场定位（答题分水岭）**："FastAPI 在我的系统里被刻意设计得很'薄'——它是**协议网关和服务门面，不是大脑**。大脑是 LangGraph 状态机，FastAPI 职责只有三件事：契约校验、协议转换、可观测埋点。业务逻辑全部下沉到图节点和策略模块——路由层薄，实现层沉，和 Tauri command 做薄代理是同一个思想。"

**四个设计维度：**

1. **契约层（Pydantic + 跨语言镜像）**：所有端点出入参都是 Pydantic 模型，放在 shared-protocol 包做 **TS⇄Python⇄Rust 三侧镜像**；例：DSpark 参数的**校验上下界定义在协议包是唯一真源**，前端不得私自收紧，否则多客户端行为不一致——契约一处定义三处执行；
2. **两类接口（REST 管操作，SSE 管流）**：请求-响应走 REST（配置/审计/会话/审批）；流式走 SSE（`/chat/{run_id}/stream` 消费状态机 astream，节点增量经映射表转类型化事件，三处同步）；**刻意的混合设计：事件走 SSE、大数据不走**——结果集超阈值走独立 WS + Arrow 列式通道，事件通道永远轻量不被万行结果卡死；
3. **状态管理（HTTP 层无状态）**：会话存 SQLite、模型注册表 router.db、轨迹 trace.db、审计 audit.sqlite；进程内只有 Runtime 单例持配置/策略表支持热重载。**无状态 = 随时可重启、可独立测试、上云可多实例**；
4. **错误与降级（每个端点都有退路）**：全局异常拦截返回结构化 detail；每个子系统有降级形态（DSpark 不可用退 Mock、审计写入 best-effort 绝不阻塞主链）；敏感配置变更端点自动写审计留痕。

**收尾金句**："FastAPI 这层我只允许它做三件事——校验契约、转换协议、留下痕迹。**任何业务判断出现在路由层，都算设计事故。**薄边界的好处：换框架只动这层，测业务不用起服务，出问题一眼分清是协议问题还是逻辑问题。"

**追问要点：**

- SSE 怎么实现？StreamingResponse + 异步生成器消费 graph.astream，经事件映射表转 event/data 帧；缓冲型事件（进程内 deque + asyncio 锁）由主流定期 drain；
- 并发怎么处理？asyncio 事件循环——LLM/工具调用都是 I/O 密集天然适合；CPU 重活（文档解析）走原生库/子进程不占事件循环；
- 为什么不全用 WebSocket？SSE 单向流/自动重连/标准 HTTP，对"服务端推事件"更简单；需要双向+大数据的通道单独用 WS+Arrow——**按数据形态选通道，不是二选一**；
- 本地服务怎么防外部访问？绑定 127.0.0.1 回环 + 前端不直连（走 Rust 代理，跨不过 CSP）——**端侧应用的"安全组"就是架构本身**；
- 端点怎么测？端点刻意薄逻辑在模块里——模块纯函数单测，端点层用 TestClient + 脚本化假 LLM 跑少量契约测试。

---

### 1.2 Agent 编排

#### Q2 双框架智能路由：ModeRouter 的实现

**结论：不是单选，是三级级联——规则优先、LLM 兜底、先验保底。**

1. **关键词规则表**（零延迟、可解释）：coding/work 双词表，单侧命中出结果，双侧命中判 `mixed`；~90% 请求在此层解决，不花 LLM 调用；
2. **LLM 分类器兜底**：受限输出三选一（CODE/WORK/HYBRID），是分类任务不是开放生成；
3. **模式先验保底**：LLM 不可用/解析失败 → 按工作模式取默认（full→coding，其他→work）。**路由节点永远有确定输出，不成为阻塞点。**

**模糊指令走查**（"帮我看看这个模块最近为什么报错"）：

- 关键词层若双侧命中直接判 mixed（模糊指令往往真是混合任务）；
- LLM 层结合 `page_context`（前端当前页签/工作模式随请求注入）消歧——同一句话在代码编辑器页签和数据工作台页签结论不同；
- **关键设计**：路由不做硬分叉，只是策略先验（决定执行纪律/校验等级），不剥夺后续节点调用任何工具的能力 → 路由错误可被下游吸收。

**四层自我纠正：**

1. 偏离声明 + 审计留痕（`mode_routed` 事件透出，用户可见可推翻）；
2. 子任务级二次判定（mixed 时逐子任务重新打 ExecutionPolicy）；
3. Auto-Repair 循环（错误反馈栈 + 次数预算 + 达上限移交人工）；
4. **红线：路由结果永不影响 HITL 风险闸门——安全性不依赖路由正确性。**

**追问要点**：词表外置 + 黄金用例回归（关键词层 ≥90%、混合分解 ≥85% 进 CI）；低置信度不反问而用"先验保底 + 偏离声明"（工作站场景打断成本高）；mode_router 定框架，intent 节点定动作，职责分离。

---

#### Q2.5 为什么选 LangGraph 作为智能体框架（Q2 姊妹题，常连着问）

**个人标签开场（差异化钩子，优先用这段开场）：**

> "选 LangGraph 对我来说几乎没有认知成本——我在信雅达做过多年 BPM 工作流（IBM FileNet、微软工作流引擎和自研流程引擎），**LangGraph 本质上就是一个给 AI 用的工作流引擎**：流程图换成有向图、流程变量换成共享状态、人工待办换成 HITL 中断、审计轨迹换成节点 trace。区别只有一个：节点执行体从'固定业务代码'变成了'概率性的 LLM'——而这恰好是我用企业级工作流的确定性纪律去驯服它的机会。"

**BPM ↔ LangGraph 概念映射表（被追问时逐条可用）：**


| BPM / 工作流引擎（熟悉的） | LangGraph 对应概念                              |
| -------------------------- | ----------------------------------------------- |
| 流程定义 / BPMN 流程图     | StateGraph（节点 + 条件边）                     |
| 流程节点 / 任务节点        | Node（一个 async 函数）                         |
| 流转网关（排他/并行）      | Conditional Edge（条件边）                      |
| 流程变量 / 上下文          | AgentState（全局共享状态）                      |
| 人工任务 / 待办            | HITL 中断点（停在节点等审批，批准后续跑）       |
| 流程实例 / 实例状态        | thread_id + checkpointer（可暂停/恢复/回放）    |
| 补偿 / 异常处理路径        | 条件边回环（repair 循环带错误回上游）           |
| 流程审计日志               | 节点级 trace + 审计库                           |
| FileNet 工作流队列 / 死信  | 多 Agent Worker Pool + 死信队列（项目里真做了） |

**防追问："和传统 BPM 最大的不同是什么？"**

> 两点：① **执行体不确定**——BPM 节点跑确定代码，LangGraph 节点里是 LLM，所以必须加硬兜底（输出校验、预算、降级）；② **控制流本身可以智能**——传统工作流分叉条件写死，这里可以有 LLM 参与的路由。分层原则：能用确定性规则的分叉绝不交给模型（三级路由第一级即此思想），只在规则覆盖不了处让模型介入。

**第一段：先列需求再给结论——框架选型看控制粒度，不是流行度。**

四个硬需求：

1. 流程里有**人工审批点（HITL）**——状态机必须能在任意节点停下等人、等完续跑；
2. 有**自修复循环**——工具失败要带错误回上游重试，这是"环"不是单向链；
3. 每步决策要**可审计可回放**——内网合规要求节点分叉全留痕；
4. 用**内网小模型**——控制流必须显式掌握在自己手里，不能藏在框架的自动推理里。

这四个需求摆出来，LangGraph（显式状态机：有向图 + 共享状态）几乎是唯一答案。

**第二段：逐个淘汰其他选项（展示调研深度）：**

- **LangChain**：链式抽象适合线性流水线；审批中断、修复回环、条件分叉用链硬做，控制流会退化成 prompt 里的自然语言约定——小模型上不可靠；
- **LlamaIndex**：定位偏检索索引层，我的检索是自己按"三层退化"策略做的，不需要它的索引抽象，编排也非主场；
- **AutoGen / 纯对话式多 Agent**：靠自由对话收敛结果，过程不可控不可审计——合规场景致命，没法向审计方解释写操作如何被决定；
- **裸写 function-calling 循环**：认真考虑过，几百行能跑，但状态持久化、断点续跑、流式分发、循环预算全要自造——这些是 LangGraph 已验证的基建，自研无差异化价值。

**第三段：LangGraph 实际给了我什么（落到实现）：**

1. **图即架构**：主图显式节点链 mode_router → intent → planner → decompose → tool_orchestrator → tool_runner → hitl_gate → repair → responder，图本身就是架构文档；
2. **HITL 是图的原生概念**：审批闸门 = 节点 + 条件边，人工介入被建模为状态机的一部分而非外挂 if-else；
3. **状态是一等公民**：所有节点读写同一个可序列化的 AgentState（路由结果、错误反馈栈、修复次数），会话恢复/跨轮上下文/测试断言有统一载体；
4. **可测试性**：节点是纯函数式（进 state 出 dict），喂脚本化假 LLM 即可跑端到端状态机测试——不确定的模型被隔离在节点内部，图的拓扑确定性可测，对内网小模型环境是决定性的；
5. **流式事件免费拿**：`astream` 按节点增量吐状态变化，转成 SSE 事件（思维链可视化、修复进度、路由徽标）；
6. **节点级审计**：每个节点套统一追踪包装器，决策进审计库——图的边界就是审计的边界。

**第四段：诚实说代价（资深感来源）：**
学习曲线陡，状态合并语义踩过坑；且它只管编排——路由策略、修复预算、审批风险分级、事件协议全是自研策略层。**结论：用 LangGraph 做确定性编排，把智能决策留在自己的策略层，边界清晰——这是生产级 Agent 系统最健康的结构。**

**概念拆解（被追问"边界清晰具体指什么"时用）：**

一句话：**"编排"和"决策"是两件事——LangGraph 负责"流程怎么走"（确定的），策略层负责"每一步怎么选"（带智能的），两者绝不混写。**

- **LangGraph 干的 = 确定性编排（铁路系统）**：节点顺序、条件边、HITL 中断/续跑、状态序列化与 astream 事件——给定相同状态结果永远一样，这层没有 LLM 没有概率；
- **策略层（dual/ 包）干的 = 智能决策（调度决策）**：ModeRouter（LLM 分类被关键词表+先验包着）、ExecutionPolicy（纯声明式表：coding 修复预算 3 / work 预算 0）、HITL 风险分级、语言硬兜底——**规律：LLM 只出现在叶子调用，从不出现在控制流**。

BPM 类比（用自己的背景讲）：**LangGraph = 工作流引擎，策略层 = 规则引擎**——当年没人把审批规则写死在 FileNet 流程定义里，同理路由策略也不该写在图定义里；唯一的时代差异是节点执行体里住着 LLM，所以策略层要多一圈确定性外壳（检测/预算/降级）。

四个实际收益：

1. **可测试**：策略层是纯函数，喂脚本化假 LLM 就能单测（若策略混在图里，这些测试一条都写不出来）；
2. **可替换**：策略模块不认识 LangGraph（只依赖 state dict），换框架/升级互不影响；
3. **可归因**：流程走错查图、决策错查策略、输出烂查兜底链——故障面被边界切开；
4. **不确定性被压缩**：全系统唯一概率源是叶子节点的 LLM 调用，图轨迹 + 策略决策记录就是完整审计证据链。

反例（不健康的写法）：把路由判断写成图里的 prompt（控制流变自然语言约定）；把修复预算写死在节点函数里（改规则要动图）；AutoGen 式自由对话（连编排层都不存在）。

点睛句："我的判断标准：**任何需要单测保障的逻辑都不允许依赖框架类型，任何流程行为都不允许依赖 LLM 自觉**。两层间唯一契约是 AgentState——框架是我的工具，不是我的架构。"

**追问要点**：

- 状态合并的坑：默认覆盖，需累积的字段（trace、error_feedback）要显式追加语义，否则节点输出冲掉历史；
- 框架 breaking change：LangGraph 只出现在图的装配层，节点是普通 async 函数——换框架成本被架构边界挡在装配层（刻意设计的解耦）；
- 为什么不用 Dify/Coze：低代码编排粒度不够（审批闸门、自修复循环做不了），且内网数据主权要求不适合 SaaS 化平台。

---

#### Q2.6 为什么 Coding 选 Plan/Execute、Work 选 ReAct（与 Q2.5 递进）

**判断框架（开场一句话）**：选编排范式只看两个变量——**任务步骤能不能提前确定**、**错误代价由什么兜底**。这两个变量在 coding 与 work 任务上完全相反，所以范式必须分开。

**Coding → Plan/Execute 的四个理由：**

1. **步骤可预知**：读代码→定位→最小修改→验证的套路稳定，计划执行前就能成型，且每个子任务可提前打执行策略（校验器级别、修复预算）；
2. **验证是确定性的**：语法/测试/编译是客观判据——有确定性验证器的任务天然适合"执行→验证→修复"闭环，而闭环需要前置计划承载上下文（改的哪个文件、第几轮尝试）；
3. **写操作风险高**：Plan 让 HITL 看到**完整的一批变更**再放行，而不是执行到一半逐个弹审批；
4. **失败代价高且可归因**：计划 + 逐阶段留痕让每个失败定位到具体步骤。

> 一句话：**Coding 的正确性靠"验证"保证，验证需要结构化计划作骨架。**

**Work → ReAct 的四个理由：**

1. **步骤不可预知（探索性）**：下一条 SQL 依赖上一条的返回，硬性预生成的计划大概率是错的；
2. **单步风险低（读为主）**：信息收集不需要重型计划，"想一步做一步"轻装上阵；
3. **外部世界不可预测**：权限/网络/数据随时变，需要"观察→分类→自适应"的即时反应——失败先分类（参数/权限/网络/数据/需人工）不无脑重试；
4. **成本效率**：大量短交互（"查下昨天订单量"），走完整规划是纯浪费。

> 一句话：**Work 的信息是逐步展开的，计划的价值低于反应速度。**

**实现层亮点（差异化考点）：没有维护两套编排器。**
主图拓扑只有一套（动态工具循环），范式差异通过**策略层（ExecutionPolicy）**表达：

- Coding 子任务：完整校验器 + 3 轮修复预算，阶段链 `code → verify`（验证是强制收尾）；
- Work 子任务：**修复预算为 0**（业务失败应分类上报而非自动重做，防重复副作用），阶段 `work → confirm`，正式外部动作前强制先产出中间产物（草稿/预览）供确认；
- 混合任务：严格 `Code → 验证 → Work → 人工确认 → 汇总`，不得混跑。

> 原因：**范式差异的本质是"策略差异"而非"机制差异"**——两套编排器 = 两套维护/测试/bug；收敛到 ExecutionPolicy 则基建只有一套，策略可单测可声明式扩展。

**追问要点：**

- Work 为什么修复预算 0？业务失败多在外部（权限/数据/对方系统），盲目重试可能重复执行有副作用的动作（重复发通知）——业务失败的语义是"分类上报"，代码失败是确定性内部错误才值得重试；
- Plan 被现实推翻怎么办？计划不是铁轨是地图：子任务仍由工具循环按实际观察推进，策略只约束元规则（验证标准/预算/审批）——计划负责结构，ReAct 负责临场；
- 为什么不全程 ReAct？正因为小模型临场规划不可靠，才把能提前确定的结构（阶段/验证器/预算）固化到确定性策略层——**把不确定性压缩到最小范围**，小模型环境的生存法则。

---

#### Q2.7 多 Agent 调度：端侧资源受限下的编排（编排域递进链收尾：路由→框架→范式→多 Agent）

**开场定位（钩子，呼应转型主线）**："端侧多 Agent 的本质，是在一台资源受限的机器上跑一个**分布式调度问题**。队列、限流、死信、重放——我在业务中台做调度服务时天天打交道的东西在这里全部重现。所以方案不是模仿学术界的'Agent 对话'，而是把成熟调度工程搬过来。"

**第一层：调度模型（Worker Pool + 队列）**：

- 主 Agent 分解出的子任务进队列，按**并发上限**派发（dispatch）——端侧没有水平扩容选项，并发数是物理约束，**限流不是优化是生存条件**；
- `run_until_drained`（排空执行）+ `cancel_all`（取消传播：父任务取消级联终止所有子任务，不允许孤儿任务继续烧算力）；
- 子任务树有结构（tree_stats）前端可视化——调度状态对用户可见，不是黑盒。

**第二层：上下文传递（核心难点）——三档策略，原则只有一条：最小必要**：

1. 简单短任务 → **passthrough**：只传结构化 input/output，不传主会话历史；
2. 中等协作 → **共享记忆池**：事实键值对带版本号，每个子 Agent **只能读自己声明过的 key**（最小授权，防上下文串味）；
3. 长会话复杂任务 → **增量摘要**：结构化摘要 + 增量 delta + 原文引用。

- 两条铁律：**必读字段（required_fields）任何策略下原样保留，压缩器无裁量权**；超长值不进 prompt，转外置引用（ArtifactRef 带 SHA-256）；
- 策略层**纯函数不依赖 LLM**，压缩率与必读字段保留率直接是 CI 指标。

**第三层：失败处理（死信队列）**：子任务失败不阻塞父任务，进 **DLQ**（可 requeue 重放 / close 关闭）；来自银行调度系统经验：**失败是常态，系统要为失败设计，而不是假装不会失败**。

**第四层：可观测性**：`correlation_id` 串联 + 事件链可回放（replay）+ 指标实时可查（metrics/queue_stats）+ spawn/progress/done SSE 实时透出。**多 Agent 最大的风险是失控，可观测性是失控的解药。**

**第五层：红线（安全不降级）**：子 Agent 写操作**照样过 HITL 闸门**，自主性再高不突破；风险分级随任务走，不随"它是个子任务"而降级——**多 Agent 放大执行能力，绝不能放大越权能力**。

**收尾金句**："端侧和云端最大的区别：云端可以靠堆机器兜住调度失误，端侧一次失控就能把用户电脑跑卡。所以整个设计围绕一句话——**把分布式调度的成熟度带进来，把分布式的资源假设丢出去**。"

**追问要点：**

- 什么时候才拆多 Agent？低耦合、可并行、单任务上下文会超限才拆（decompose 节点决策，有 multi_agent 标志）——**拆是成本不是炫技**；
- 怎么防成本失控？预算随任务传播（深度限制 + 轮次上限 + token 预算），父预算是子预算之和的上界，超限终止不硬撑；
- 为什么不用 AutoGen 自由对话？过程不可控不可审计，合规场景无法解释"写操作是谁决定的"；队列式调度每步都有 correlation_id 留痕；
- 大结果集回传？不走 SSE/JSON：超阈值走 WebSocket + Arrow 列式批（元数据帧/数据帧分离），避免卡死事件通道；
- 子任务结果冲突？父任务汇总节点裁决，冲突显式上报用户而非模型私自调和——不确定就暴露，不掩盖。

---

#### Q2.8 意图识别：五级递进管线（编排入口节点：路由是第零站，意图是第一站）

**开场划边界**："mode_router 决定'按什么框架做'，intent 节点决定'用户到底要什么'——前者是策略分叉，后者是语义理解。设计原则和路由一样：**快路径优先、逐级降级、永不阻塞**。"

**五级管线（核心）：**
0. **空输入短路**：无 prompt 直接判闲聊，零调用；

1. **意图向量快速路由（零 LLM）**：semantic-router 模式，embedding 与预置路线比对命中直出；未命中/不可用静默回退，**快速路径故障绝不影响主链路**；
2. **结构化意图分析（主力）**：输出不只是标签，是一份结构化分析——四分类（query/mutate/orchestrate/chitchat）+ 细分类型 + **改写句**（模糊提问改写成规范表述供下游）+ 实体 + **缺失字段** + 是否追问（带话术）+ **风险等级** + 置信度；
3. **宽容解析**：小模型 JSON 用 from_raw 宽容解析：非法字段丢弃、缺失补安全默认、置信度截断 [0,1]——意图分析本身也遵守解析容错哲学；
4. **旧式四分类回退**：结构化不可用退回纯四分类，置信度策略性给 0.6——**刚好过下游 decompose 快速路径门槛**，明确操作型意图不再花一次编排决策器调用。

- **并行支线：技能路由**——同时跑技能匹配（LLM 优先 + 关键词回退），命中激活技能，不命中静默跳过。

**两个差异化细节（加分点）：**

1. **页面上下文注入消歧**：前端当前页签随请求注入；真实案例：用户停在"模型接入配置"页签说"帮我连接"——没上下文是歧义动词，有上下文直接知道连什么——**用场景信号消歧，不逼用户把话说全**；
2. **意图输出直接驱动安全**：risk_level 和 mutate 判定流向 HITL 层——**意图识别不只服务理解，是风险闸门的第一道信号源**。

**可观测性**：每步写 CoT 日志 + 持久化轨迹（走了哪级/命中哪条路线/耗时/置信度）——识别错了能在轨迹里定位是哪级出的错。

**收尾**："和路由完全相同的工程哲学：**能零成本解决的绝不花 LLM，必须花 LLM 的地方输出结构化而不只是标签，任何一级失败都有下一级兜着**——意图节点永远不成为请求阻塞点。"

**追问要点：**

- 为什么不只用一次 LLM 分类？一次分类只有标签，拿不到改写句/缺失字段/追问/风险——这些是下游规划和澄清的原料；且纯 LLM 没快路径，高频问法每次花全价；
- 置信度怎么用？门槛驱动行为：≥0.6 走分解快速路径，以下进编排决策器——**置信度不是展示数字，是调度信号**；
- 识别错了下游怎么办？改写句让人类可核对（澄清卡片）；mutate 误判最坏是多走一道审批（宁可错审不可漏审）；全链路轨迹可回放定位错因；
- 多轮上下文怎么处理？分析带历史消息（有截断预算），改写句的职责之一是**指代消解**——"再查一下那个表"改写为完整表述给下游。

---

### 1.3 工程鲁棒性

#### Q3 小模型解析容错（Parser Robustness）

**总策略：先让解析问题消失，剩下的宽容解析 + 确定性兜底，而不是堆 Grammar。**

1. **收窄输出空间**：路由分类不要求 JSON，只要一个词（CODE/WORK/HYBRID），`lower()` 后 token 包含匹配——输出空间从"结构化文档"收窄到"单枚举词"，格式错误在设计阶段被消掉；
2. **宽容清洗 + 等价归一**：剥离 `<think>` 内心独白（真实踩坑：推理模型把思维链塞进 final_answer）、markdown 围栏剥离；HYBRID↔mixed 语义等价映射；
3. **需要严格结构处走原生协议**：工具调用每 run 首节点探测原生 tool calling 支持，支持走原生（格式由推理层保证），不支持退提示词协议 + JSON 提取；探测失败绝不影响主链路；
4. **兜底哲学——不盲目重试**：路由这类高频低代价有退路的调用，解析失败直接回退先验（重试延迟翻倍且小模型抽风常是系统性的）；只有工具参数解析失败才进 Auto-Repair（错误塞回 prompt + 预算）。

**为什么不用 Outlines/llama.cpp grammar**：① 锁死推理后端（要适配 Ollama/OpenAI 兼容/原生多种）；② grammar 只保证语法合法不保证语义正确，校验一层省不掉；③ 内网小模型在 grammar 约束下延迟失败率上升。结构化约束做在协议设计层，兜底做在解析层，中间不引入重依赖。

---

#### Q3.5 HITL 移交的认知负担（HITL UX）

**核心认知：3-5 秒理解现状不是弹窗出现那一刻开始的，而是从第一次修复失败就开始了。**

1. **过程透明**：每次修复尝试实时推 `repair_attempt` 事件，前端渲染"Auto-Repair 第 2/3 次修复中…（错误摘要前 80 字符）"——移交时用户已看着系统挣扎三轮，认知负担摊薄到全过程；
2. **结构化摘要，绝不给原始日志**：
   - 工具循环产出确定性移交文案（不走 LLM 生成，防失败状态下说胡话）；
   - 最终回复附结构化报告：结果状态 + 最后一条错误摘要（截断 200 字符）+ 涉及文件清单——只给"现在卡在哪"不给全量尸检；
   - **红线：无验证信号绝不生成"已验证"字样，防失败时伪造成功状态**；
3. **决策降维——让用户"选"不让用户"想"**：审批卡片给候选方案列表，推荐项高亮标星，每选项附调整后计划 + 风险注记 + 推荐理由；**三值按钮：批准/驳回/问询**——"问询"承认用户可能看不懂，给低成本出口；高风险叠加 MFA/双人复核。

**追问要点**：error_feedback 栈上限 6 条防 token 爆炸（审计全量留痕）；移交后新指令从已有反馈栈续跑不从零开始（所以放状态不放局部变量）。

---

#### Q3.7 系统验证体系：指标与案例（鲁棒性域收尾：解析容错 → HITL 体验 → 验证）

**开场验证哲学**："LLM 系统验证的核心矛盾：模型输出不确定，但交付质量必须确定。解法不是端到端黑盒测模型，而是建一座**测试金字塔**——把不确定性逐层隔离，每层用最合适的验证手段。"

**六层验证体系（手段 + 指标）：**

1. **L1 纯逻辑单测**：路由关键词表/ExecutionPolicy/上下文策略是纯函数不依赖 LLM；**压缩率、必读字段保留率直接是 CI 断言**——压掉必读字段测试直接红；
2. **L2 状态机端到端**：`_ScriptedLLM`（脚本吐预设动作）+ `_FlakyMCP`（脚本故障）跑完整图——模型脚本化后状态机就是确定性的，测行为：失败一次→修复→收尾；失败十次→到上限放弃并如实报告；
3. **L3 黄金用例集**：路由关键词层准确率 **≥90% 是 CI 硬门槛**，混合分解 ≥85%；新发现的误判指令随时补进用例集——用例集是活的回归资产；
4. **L4 真模型集成**：单独标记不进快速回归（慢/不稳/耗算力），发版前/定期跑；快速回归靠 L1–L3，真模型管最后一公里；
5. **L5 跨语言契约**：事件名三处同步专门测试 + shared-protocol round-trip——三语言系统契约必须机器兜底；
6. **L6 性能基准**：DSpark 独立场景集**双门槛**（加速比 ≥1.5× 且接受率 ≥60%），基线对照跑；
7. **L7 生产可观测**：审计可回放（SQLite）+ compression_log + 决策环形缓冲——**测试管上线前，可观测性管上线后**。

**三个具体案例（按追问深度选用）：**

- **A（输出稳定性）**：中文硬兜底 **11 条回归测试**覆盖边界——英文终答触发重写、重写失败回退原稿、含 clarify 卡片不触发、拉丁字符阈值边界，每条对应一次真实踩坑；
- **B（修复链路）**：e2e 里 `_FlakyMCP` 故障一次→断言修复恰好 1 次且走 summarise 收尾；故障十次→重试恰好到上限后放弃，且最终回复是**确定性失败文案（不经 LLM，防失败状态说胡话）**；
- **C（回归防线）**：LIMIT 双重注入是真实 bug 修出的回归用例——确认回填必须用原始 SQL，钉死"前端不参与 SQL 改写、后端是唯一注入方"的边界；规矩：**每个 bug 修复必须留下至少一条测试**（BUGFIX 记录 130+ 条对应 130+ 条回归防线）。

**指标汇总表（被要数字时直接报）：**


| 类别       | 指标                                                                      |
| ---------- | ------------------------------------------------------------------------- |
| 准确率门槛 | 路由关键词层 ≥90%、混合分解 ≥85%（CI 断言）                             |
| 性能门槛   | 推测解码 ≥1.5× 且接受率 ≥60%（双门槛）                                 |
| 工程质量   | 三栈 CI 全绿：pytest + ruff + mypy --strict / vitest / clippy -D warnings |
| 契约       | 协议 round-trip + 事件三处同步测试                                        |
| 回归资产   | 38+ 测试文件、130+ BUGFIX 对应回归用例、11 条硬兜底边界用例               |
| 生产观测   | 审计可回放、压缩日志、决策环形缓冲                                        |

**收尾金句**："能确定性化的行为全部用确定性测试包住；包不住的留指标在审计日志里让生产数据说话。**测试不是为了证明系统能跑，是为了让每一次'跑不了'都发生在上线之前。**"

**追问要点：**

- 黄金集谁标注？手工标注 + 滚动积累（误判指令随手补），成本分摊在日常不是集中工程；
- 内网没真模型怎么跑回归？这正是脚本化假 LLM 的价值——CI 完全离线可跑，真模型测试单独标记延后；
- 怎么测体验/前端？组件走 vitest（流聚合/卡片渲染/点击行为），体验链路走手工走查 + 录屏——无法全自动的部分就流程化；
- 测试慢不慢？分层隔离的意义就在这：L1–L3 秒级每次提交跑，L4 发版前——**回归速度和覆盖度是分层买来的**。

---

#### Q3.8 评测集设计（质量域收尾：容错 → 体验 → 验证 → 评测）

**开场定位**："评测集是**概率系统的单元测试**——代码对错有编译器管，模型行为好坏必须有评测集管。四条设计原则：**分层、可门槛化、与系统隔离、滚动进化**。"

**项目里的三个真实评测集：**

1. **路由黄金用例集（能力级）**：用例 = (指令, 期望路由)；分层覆盖明确 coding/明确 work/混合/模糊/无关（"今天天气怎么样"不应武断命中）；准确率 **≥90% 写成 CI 断言**——评测集不是报告工具，是质量闸门；来源是活的：人工标注打底 + 真实误判滚动入库；
2. **混合任务分解集（链路级）**：用例 = (任务+计划, 期望阶段序列)，如"写导出脚本再跑报表"→ code→verify→work→confirm；测的是编排决策正确性，门槛 85%；
3. **性能基准场景集（非功能）**：用例 = (任务类别, prompt, max_tokens, 期望模式) + 基线对照；双门槛验收（≥1.5× 且接受率 ≥60%），输出结构化 JSON 报告——**评测必须可复现、可归档、可追溯**，不是"跑了一下感觉快了"。

**通用设计方法论（升维，展示可迁移能力）：**

1. **一条用例五要素**：输入、期望输出（或可接受范围）、度量指标、**归因标签**（出错时定位到哪层）、来源——归因标签最易被忽略但决定评测失败时能否快速定位；
2. **分层**：单元能力（分类/路由）→ 链路行为（修复/审批）→ 端到端质量；越下层越确定性指标，越上层越接受概率性；
3. **指标跟任务形态走**：分类用准确率/混淆矩阵；结构化输出用字段校验；生成任务**尽量转确定性判据**（引用可定位/语言合规/格式正确），残余才用人工抽检或 LLM 评审；
4. **隔离纪律**：评测集不进 prompt、不与系统日常数据同源——防"对着答案学习"式过拟合；快回归（规则/脚本秒级）与慢评测（真模型发版前）分开，成本分层；
5. **闭环进化**：每个线上 badcase/用户投诉最终都要变成一条用例——**评测集的厚度就是系统被纠正的次数**。

**诚实边界（资深感）**：开放式生成最难评——不追求完美指标，而是**拆成多个可判定子项**（事实有无检索依据/语言合规/格式正确），每项都是确定性的；真只能主观判断的部分留定期人工抽检。**把能确定的部分全部确定化，是评测设计的核心功夫。**

**收尾金句**："评测集不是给模型打分的考卷，是给系统立的**质量宪法**——门槛写进 CI，坏案例滚动入库，任何人随时能验证系统今天的水平。"

**追问要点：**

- 怎么防评测集作弊/过拟合？三道防线：评测数据不进 prompt、与回归集角色分离、定期用新 badcase 换血——评测集要有"新鲜度"概念；
- 用例量多少够？不求数量求覆盖结构：每个意图类别/歧义形态/失败模式都有代表用例；百级高质量胜过万级无标注；
- LLM-as-judge 怎么看？用过但要设防：评审模型自身偏差（偏长/偏华丽）会污染评分，只用于确定性判据覆盖不了的残余，且评审提示词本身要做一致性校验；
- 评测集过时怎么办？两个触发信号：线上出现未覆盖的新误判模式；系统升级后老用例的期望行为本身变了（用例跟产品定义走，不刻舟求剑）；
- 端侧特殊性？评测要在**目标硬件**上跑才有意义——同一模型在 CPU/NPU 上延迟/接受率完全不同，性能基准带硬件维度——这也是反问环节问"真机资源"的原因。

---

### 1.4 RAG 与推理优化

#### Q4 MACC 三层自适应压缩 + 防幻觉文本定位

**MACC 三层（按"离原始文本的距离"分层）：**

- **L1 物理层**：KV Cache 压缩，本层只标记，由 DSpark 在 llama.cpp 侧接力执行；
- **L2 表示层**：Gist Token 式多模态压缩（诚实说明：目前是占位设计，无真编码器）；
- **L3 逻辑层**（实装主力）：工作记忆（滑动窗口原文）+ 情景记忆（事件图节点+边）+ 语义记忆（蒸馏规则带置信度，同规则去重累加）。

**自适应 = 策略分发矩阵**（CompressionRouter 按 token 数/消息数/多模态/空闲时长选六策略）：


| 画像                | 策略                                   |
| ------------------- | -------------------------------------- |
| 短对话（<8K）       | NONE，原文全量——不压缩就是最好的压缩 |
| 中等（8K–32K）     | WORKING_ONLY，锚点 + 滑动窗口          |
| 长会话（32K–128K） | MEMORY，L3 三层全上                    |
| 超长（>128K）       | HYBRID，打 L1 标记交 DSpark            |
| 含多模态            | GIST                                   |
| 空闲 >5 分钟        | 后台触发 MEMORY 整理（不打扰交互）     |

两条纪律：**L3 只存元数据不存原始对话**（合规红线）；**每次压缩写 compression_log**（可观测性 + 未来策略自学习训练数据）。

**高频追问："上下文压缩不能删什么？"**

判断原则：**压缩的对象是"表述"，不能压缩的是"事实和状态"。**表述可摘要，事实被压掉系统就会基于虚构状态继续跑。

落地实现：项目里有架构师约定的**锚点表（DEFAULT_ANCHORS）**——滑动窗口怎么滑，锚点变量永远保留。四类不可压信息：

1. **HITL 审批状态**（approval_id、pending_decision）——压掉待审批状态，轻则审批流断掉，重则系统"忘了"写操作未获批就执行——安全状态丢失是灾难级的；
2. **最近一次工具执行的事实**（last_tool_name、last_tool_status、affected_rows）——"失败"和"影响 3 万行"这类精确事实不能被措辞抹掉；
3. **错误反馈栈**（repair_attempt、error_feedback）——自修复循环的燃料，压掉会让模型重复走已失败路径；
4. **用户原始意图**（intent 层）——长会话压缩最常见事故是目标漂移，原始指令必须全程携带。

三条通用判定标准（跳出项目层面）：

1. **事实不可压，表述才可压**——观点/寒暄/重复解释可摘要；数字、状态枚举、执行结果必须原样保留；
2. **影响安全与审批的状态不可压**——与"操作是否已授权"相关的信息宁可冗余不能丢；
3. **显式声明的必读字段不可压**——多 Agent 铁律：ContextPolicy.required_fields 在任何压缩策略下原样保留，压缩器没有裁量权。

设计纪律：**压缩 ≠ 蒸发**——超长内容转外置引用（ArtifactRef，带 SHA-256），原文随时可回查，让"压缩错了"成为可修复错误而非不可逆损失。

防追问要点：

- 怎么保证压缩器遵守？锚点在压缩后**无条件拼回** formatted_prompt（代码层先于窗口处理，不靠压缩逻辑自觉）；多 Agent 侧有测试断言 required_fields_kept，压掉必读字段直接测试失败；
- 锚点表怎么维护？声明式（节点名 + 变量名列表），新增一行即可，是架构约定不是散落硬编码；
- 摘要把关键数字摘丢了怎么办？事实类字段走结构化提取进锚点/共享记忆池，**不信任摘要文本承载事实**——摘要只负责叙事，不负责精确值。

**防幻觉文本定位——设计立场：不是"生成后检测幻觉"，而是"让生成没有机会幻觉"：**

1. **三层退化检索，精确优先**（财税文档审核为例）：① grep 精确子串匹配——每条依据必须在原文精确匹配到位置（kb_refs），高准度优先；② BM25（CJK bigram 分词，TF 饱和+IDF，得分归一化混合）；③ 本地 embedding 语义召回（可降级，没配服务自动退化纯 BM25）。**顺序故意反着来：能精确匹配绝不用模糊召回——合规场景要"第几条第几款"不是"语义相近段落"。**
2. **引用即约束**：风险结论必须携带检索返回的原文定位，无检索凭据不允许成为 finding——把幻觉问题从"检测模型编没编"转为"有没有检索凭据"的确定性校验。
3. **原则贯穿全系统**：codenav 跨文件跳转只允许符号索引精确命中，未命中返回 not_found，绝不让 LLM 猜位置——宁可诚实的"找不到"，不给看似合理的错误位置。
4. **分层标注**：有原文依据标"依据型结论"，模型推断标"提示型建议"，永不混装可信度。

---

#### Q4.5 记忆系统设计（Q4 姊妹题：压缩 → 记忆；联动 Q6 C 端场景）

**开场设计立场**："记忆不是'把所有对话存下来'——那是日志，不是记忆。人的记忆分层：此刻在想的、经历过的事、沉淀成的常识。我照这个结构建三层，并先定红线：**什么不该记，比记什么更重要。**"

**三层记忆（每层：存什么/怎么召回/生命周期）：**

1. **工作记忆（此刻在想的）**：锚点（关键状态变量：审批状态/上次工具结果/用户原始意图）+ 滑动窗口（最近 N 条，单条截断）；**锚点永不随窗口滑走**——窗口管新鲜度，锚点管不能丢的事实；随会话，压缩时最先被重组；
2. **情景记忆（经历过的事）**：**事件图**——节点是事件（谁/对什么实体/做了什么），边是关系（先后/触发），不是流水账；召回从命中节点做 **BFS 扩展**（沿 outgoing+incoming）捞关联事件链；用图不用列表因为"上次改的那个表后来怎样"需要关联，线性历史做不到；
3. **语义记忆（沉淀成的常识）**：蒸馏规则（如"订单平账先确认 payment status"）带 pattern + 置信度；写入**同规则去重、置信度累加**——多次出现才成常识，单次偶发不算；召回关键词命中 + 置信度加权排序，低于阈值不进上下文。

**蒸馏链（设计灵魂）**：情景记忆在空闲时段（无交互 >5 分钟）后台把重复事件模式蒸馏成语义规则——**经历变成常识**；刻意放后台（不与实时交互抢算力），完成通过 `memory_consolidated` 事件透出，全程可观测。

**红线与纪律**：① **只存元数据不存原始对话**（实体/动作/摘要入库，原文不进记忆库，敏感环境合规红线）；② 事实类信息走锚点结构化存储，不信任摘要文本承载事实；③ 每次压缩/蒸馏写 compression_log（策略/压缩比/耗时留痕，也是未来策略自学习的训练数据）。

**C 端收尾（联想语境用）**：写入蒸馏后台跑不扰民，召回是轻量检索不耗算力，而**记忆是本地独占的**——云端因隐私不敢做深度个人记忆，这是端侧护城河；用得越久越离不开（留存引擎）。

**追问要点：**

- 怎么防记忆污染（错误记忆被强化）？三道闸：置信度累加需多次出现（偶发不成常识）、召回有 min_confidence 阈值过滤、规则带来源会话可追溯可清理——诚实说衰减/冲突消解是下一步；
- 记忆多了召回怎么选？关键词命中 + 置信度加权取 top_k，且有注入预算（token 上限）——记忆进上下文是有配额竞争的，不是无限灌；
- 和 RAG 什么区别？同一套检索基建，不同生命周期：RAG 管外部文档（静态、可引用原文），记忆管用户历史（动态、要蒸馏要遗忘）——一个是查书，一个是回忆；
- 为什么不用向量库存记忆？会话级记忆是百到千级结构化条目，关键词+置信度检索足够且零基建；向量适合十万级静态文档，不适合高频变更的个人事件流；
- 情景记忆用图的代价？诚实答：图比平面表复杂、维护边关系有成本；收益是关联召回——"上次那个事后来怎样"只有图能答。

---

#### Q4.6 自进化设计：不动权重的知识闭环（Q4.5 递进：记忆 → 进化）

**开场划边界（答题分水岭）**："我的系统**从不微调模型权重**——内网端侧持续训练既不现实也不合规。进化发生在**模型之上的三层：知识层、策略层、数据层**。一句话：**经验 → 知识 → 召回 → 验证 → 沉淀**的闭环，且闭环本身必须可治理。"

**核心机制：记忆蒸馏链（经验变知识）：**

1. **采集**：会话关键操作记为事件图谱（谁对什么实体做了什么）；
2. **蒸馏**：后台任务定期扫描，**同一 (实体,动作) 模式出现 ≥阈值（默认 3 次）才蒸馏成规则**——偶发不变常识，防污染第一道闸；置信度频次归一化（count/10 封顶 1.0）；
3. **沉淀**：规则入语义记忆表，**同规则去重、置信度累加**，每条带 source_event_ids——任何"学到的知识"可回溯到具体哪几次经历教出来的；
4. **应用**：召回关键词命中 + 置信度加权，**低于阈值不进上下文**——学过的不一定用，可信的才用。

- 版本路线：V1 启发式（频次统计，零 LLM 成本、完全确定性）先把闭环跑通；V2 接 LLM 做语义泛化——**先用确定性方法跑通，再把智能加进来**；触发在空闲后台，进化不和交互抢算力。

**数据飞轮（三层数据资产供料）：**

- 压缩日志：每次压缩策略/前后 token/比率留痕——设计标注为未来策略自学习（甚至 PPO）训练数据；
- 推测解码决策记录：环形缓冲为按接受率动态调参准备；
- 黄金用例集：路由每误判一次真实指令就补一条——**系统被纠错一次就强壮一次**。

**工程层进化（最容易被忽略但最有说服力）**：制度化——**每个 bug 修复必须留下至少一条回归测试**（130+ BUGFIX 对应 130+ 回归防线）；HITL 用户审批决策全部留痕，成为自动模式决策矩阵的训练数据——**人教系统一次，系统记一辈子**。

**红线：自进化本身必须可治理**：规则来源可追溯、支持显式删除、召回有置信度门槛、蒸馏有频次下限——进化系统每个产出可审计可回滚。**可控的进化才是工程，失控的进化是事故。**

**收尾金句**："我的自进化是'不动权重的知识闭环'——**模型是租来的，知识是自己的**；模型不会变聪明，但系统越来越懂这个用户。这也是端侧产品的商业逻辑：进化沉淀在用户本地，就是留存护城河。"

**追问要点：**

- 为什么不做在线微调/RL？端侧三重约束：算力不允许、内网合规不允许数据汇总、小模型微调收益不稳；知识层进化成本低一个数量级且完全可控——**在正确的层做正确的事**；
- 怎么防学错（错误经验固化）？四道闸：频次下限、置信度累加而非一步到位、召回阈值过滤、来源可追溯可删除；V2 加人工审核入口；
- 进化效果怎么度量？蒸馏条数/规则命中率/召回注入率都在日志；诚实说端到端体验度量是下一步，目前先保机制正确、数据留全；
- 跨用户/跨设备进化？**端侧隐私前提下不做跨用户共享**——知识沉淀本地，既是合规红线也是卖点（"你的 AI 只属于你"）；
- 启发式蒸馏是不是太简单？主动承认 + 路线：V1 价值是把闭环跑通且零成本零幻觉；频次模式覆盖高频操作类知识；语义泛化（"订单平账先查支付状态"类）是 V2 接 LLM 的目标。

---

#### Q5 DSpark 推测解码（llama.cpp）

**模型选择（三原则）：**

- Target：Qwen2.5 系主模型（GGUF 量化）；Draft：**qwen2.5-0.1b-instruct Q4_K_M**；
- ① 同家族优先（同 tokenizer 同分布，接受率天然高）；② 尺寸比 ~1/5，保证草稿开销 ≪ 验证开销；③ 端侧约束：内网机器无富余显存，0.1B Q4 百 MB 级可共存于 CPU 内存。
- **数学保证**：Leviathan 2023 拒绝采样，输出分布与 target 自回归严格等价——加速不牺牲质量，合规场景敢用的前提。

**测量方法**：独立 bench 脚本，同场景跑两遍（n_draft=1/p_min=1.0 为基线），加速比 = 基线耗时 / 实验耗时；从 llama.cpp usage 取 drafted/accepted tokens 算接受率；**双门槛：加速比达标且接受率达标**。

**数字（诚实口径，已与代码核实）**：1.5× 是基准测试的**双门槛验收标准**（加速比 ≥1.5× 且接受率 ≥60%，配套独立场景集），不是实测均值——口径说成"验收标准"反而展示量化体系；典型任务落在 1.5–2 倍区间；结构化输出任务（SQL/代码）接受率最高收益最大；纯 CPU 别指望论文 GPU 上的 3x。

**四道防"负优化"闸：**

1. 短输出跳过（max_tokens < 阈值默认 20）——收益摊销不了草稿开销；
2. n_draft < 2 强制关闭（测试断言）；
3. 按任务类别四档策略：激进（n_draft=8, p_min=0.75）/标准（4, 0.85）/保守（2, 0.90）/关闭（1.0）——接受率低的任务宁可不开；
4. `_LOCAL_ONLY_TASKS` 敏感任务硬关闭——策略永远让位安全红线。

**兜底哲学**：draft 懒加载；加载失败/llama.cpp 没装/生成抛异常全部静默降级主模型单独运行——**加速是可选项，正确性不是**。

**追问要点**：接受率低动态调参是下一步（决策留痕环形缓冲 + 前端面板已就绪——先可观测再自适应）；L2 不做因端到端 gist 编码器需专门训练，用"结构化摘要 + ArtifactRef（SHA-256 可追溯）"替代；压缩不丢关键信息靠锚点 + 必读字段不可压铁律。

---

## 第 2 章：产品、场景与行为面试

### 2.1 产品设计题

#### Q6 C 端本地 AI 助理：优先落地哪三个场景

**选择标准：该场景是否"本地化"本身构成购买理由——云端做得一样好就不值得预装。**

**场景一：私人文档助理**（合同风险/保单解读/文档问答）

- 痛点：合同、保单、体检报告等敏感文档不敢传云端；
- 本地化优势：不是"本地也能做"而是"只有本地才敢做"——隐私是准入门槛；
- 算力匹配：RAG 把范围收窄到几个段落，7B 量化模型只做"基于给定材料总结"的受限任务；解析检索全在 CPU。digPref 的解析→分块→三层检索→出处引用链路可原样搬用。

**场景二：屏幕随手问**（截图理解：缴费单/体检报告/报错弹窗）

- 痛点：消费者真实行为是"截图发给朋友问"——替代这个动作；
- 本地化优势：屏幕内容天然含隐私，"截图上传"本身就劝退；
- 算力匹配：7B 多模态单图理解够用；单次短交互，15 tokens/s 可接受。复用视觉理解链路 + 页面上下文注入。

**场景三：个人记忆与日常纪要**

- 痛点："上次快递单号是多少"/周报年终总结——云端助理能做事但不敢记你；
- 本地化优势：**长期个人记忆是端侧独占护城河，用得越久越离不开（留存引擎）**；
- 算力匹配：写入蒸馏放空闲后台跑（MACC 空闲 5 分钟触发机制），召回是轻量检索。

**取舍陈述**：写代码/查数据库不进首批——C 端密度低且最吃算力。三场景共同点：隐私敏感度高、单次交互短、可复用已验证链路。

---

#### Q7 端侧算力瓶颈下的体验保卫战（TTFT 4-5s，15 tokens/s）

**先拆问题**：首字延迟（TTFT）是主战场（看起来像死机）；15 tokens/s 中文 ≈ 快速打字员速度，流式输出下可接受。

**产品交互层（管理感知时间）：**

1. 阶段化进度拒绝空白："正在理解问题 → 查找资料 → 生成回答"，真实信号驱动（思维链可视化思路）；
2. 先给骨架再长血肉：先出一句话结论（1-2 秒）再展开——用户判定"有反应"的标准是第一个字；
3. **把延迟重新叙事成隐私卖点**："正在你的电脑上本地计算，数据不会上传"——云端叫慢，本地叫安心；
4. 高频问题走确定性逻辑/小模型毫秒级返回，锚定速度印象；
5. 长任务异步化：明确告知耗时 + 完成通知 + 允许离开——焦虑来自"被绑住"不是耗时。

**工程架构层（管理真实时间）：**

1. 模型常驻 + system prompt 前缀缓存（KV cache 复用）；
2. **分级模型路由**：简单闲聊给 0.5B/1.5B（首字亚秒级），复杂任务才给 7B——C 端 80% 是简单请求；
3. 上下文压缩降 prefill（MACC 从省钱工具变降延迟工具）；
4. 推测解码压生成时间（结构化输出场景）；
5. 语义缓存秒回高频重复问题（C 端重复率远高于 B 端）；
6. NPU 调度（OpenVINO/llama.cpp NPU 后端）——"模型在算的时候电脑还能用"。

**收尾金句**：交互层管感知时间，工程层管真实时间，感知层见效快先做；本地产品独有底牌是把等待讲成隐私故事。

---

### 2.2 行为面试题

#### Q8 转型动机与职业规划

**转型逻辑（拉力不是推力）**：

> 做架构师时发现：过去十年解决的问题——编排、路由、状态机、审批闸门、熔断降级——在 AI Agent 里全部重新出现，只是对象从"微服务"变成"模型"。业务中台的"统一报文协议 + 三段式模板 + AOP 切面"和 digPref 的"LLM 路由 + HITL 闸门 + 审计切面"架构思想同构。
> **我不是放弃了过去，是带着过去的武器去了新战场。**digPref 就是给这个判断下的注。

**两大阵痛（要具体诚实）**：

1. **思维范式切换**：Java 教会"一切必须确定"，但 LLM 是概率系统。踩坑后悟出：对小模型 prompt 是软约束必须配硬兜底（输出校验/确定性检测/降级）。把 Java 纪律用对地方——不信任模型如同不信任外部系统：加校验、加超时、加兜底。
2. **知识密度焦虑**：克服方法——**不追概念追"实现一遍"**，每个模块写成能跑的代码 + 测试。"写不出测试就说明没真懂。焦虑的反义词不是自信，是手上有活。"

**3-5 年定位**：端侧/本地化 AI 应用技术负责人——技术深度为主、团队执行为辅。模型能力往小模型下沉，"受限硬件上把 AI 做成可靠产品"的工程体系几乎空白，这是结构性位置。核心竞争力在"把不确定性工程化"，管理价值来自仍站技术第一线。

---

#### Q9 跨团队冲突破局（首发语音/视频 vs 端侧不达标）

**定调**：这不是对错问题，是"需求定义问题"伪装成"立场冲突问题"。产品经理背后有真实目标（发布会炸点/竞品压力），算法团队要的是"别背达不了的指标"。**我的职责不是裁判，是把双方隐藏目标挖出来找同时满足的路径。**

**四步处理：**

1. **分别一对一倾听挖隐藏目标**（当天）：僵持时不开会（只会强化立场）。经验：十次僵持八次挖到底发现双方要的不是同一个东西；
2. **把"能不能做"翻译成"分几步做"**（技术破局）：
   - 拆链路：10 秒端到端拆成 ASR/LLM/TTS 三段，**流式化流水线并行**，延迟从总和降到接近最长一段；
   - 分级体验：首发改"语音消息模式"（按住说话 3-5 秒，工程可行），"实时对话"作 v1.1 预告——**发布会炸点首发就能拿到，达标也守住**；
   - 视频理解降级首发"拍照问答/单帧理解"（7B 够），视频进路线图——用户价值少 20%，工程难度差两个数量级；
3. **带方案和数字开三方会**：一页纸 = 双方目标 × 方案满足度 × 实测延迟与达标判据，目标是让双方在"分级交付路线图"上签字。话术："不是砍需求，是把 100 分和 60 分分开交付——首发当天被骂'慢得像幻灯片'比晚上线两周伤害更大。"
4. **建机制防复发**：体验红线前置（交互类功能端到端 ≤2 秒写进立项标准）+ 算法团队月度能力基线测试——**把冲突从"临场博弈"变成"规则内决策"**。

**收尾原则**："技术负责人最大的忌讳是用技术术语碾压产品，或无底线妥协换和气。正确姿势：**对目标强硬，对方案灵活。**"

---

### 2.3 反问环节（1-3 个）

**反问 1（战略层）**："咱们助理产品和 NPU 芯片、整机功耗等硬件团队的协同深度如何？能拿到目标 NPU 的推理工具链支持吗？这款产品是独立用户产品还是'AI PC'战略的能力底座？"

- 听答案：硬件是两张皮则优化天花板低；定位答不清说明战略摇摆期。

**反问 2（岗位层）**："半年和一年时，您心里'做得超出预期'分别是什么样子？有量化锚点吗（首字延迟/日活/场景准确率）？团队算力测试资源如何——目标真机人手一台还是排队共享？"

- 听答案：答得出具体画面 = 想清楚了的团队；真机资源是端侧团队照妖镜。

**反问 3（团队层，时间够再问）**："团队有没有技术分享、允许工程师做'不直接产出功能'之事的空间？端侧推理优化这块目前最强的人是谁、背景如何？"

- 听答案：对"慢功夫"皱眉 = 纯交付型团队；说不清技术构成 = 可能单兵作战（是机会也是风险）。

**收束话术**（峰终定律）："今天聊下来更有信心了——您关心的端侧算力约束、幻觉控制、体验优化，恰好是我过去一年半每天在解的题。期待有机会共事。"

---

## 第 3 章：高概率问题速查表（未深挖领域，防偷袭）

### MCP 与工具层


| 问题                              | 应答锚点                                                             |
| --------------------------------- | -------------------------------------------------------------------- |
| 为什么用 MCP 而不是直接函数调用？ | 标准协议解耦；工具进程崩溃不影响主链路；四个 Server 按需加载不占内存 |
| database MCP 怎么防删库？         | SQL 拦截器硬阻断写操作 + 表白名单沙箱 + 读操作强制注入 LIMIT         |
| 工具超时/挂了怎么办？             | 带超时；失败结果结构化喂回工具循环走 Auto-Repair                     |
| 内置工具 vs MCP 边界？            | 高频低延迟进程内做内置；重依赖需隔离的（DB 驱动/SSH）做独立进程      |

### 安全与凭证


| 问题                   | 应答锚点                                                                 |
| ---------------------- | ------------------------------------------------------------------------ |
| 凭证怎么存？           | 只走 OS Keyring，内存磁盘不落明文，日志严禁打 DSN                        |
| 敏感任务为何强制本地？ | `_LOCAL_ONLY_TASKS` 白名单硬路由，安全策略不信任概率性决策               |
| 审计记什么？           | 路由决策/审批动作/工具调用/写操作前后镜像，SQLite 可回放，自动决策也留痕 |

### NL2SQL 数据专家


| 问题                          | 应答锚点                                                                                         |
| ----------------------------- | ------------------------------------------------------------------------------------------------ |
| 怎么保证生成的 SQL 正确？     | 业务字典外置 YAML 注入 + 执行前 HITL 确认 + 失败报错喂回重试——不追求一次完美，追求错误可见可修 |
| 确认过的 SQL 重复注入 LIMIT？ | **踩坑金句**：确认回填必须用原始 SQL 而非后端改写版，否则双重注入                                |
| 大结果集传输？                | 超阈值走 WebSocket + Arrow 列式批传输，不走 JSON 避免卡死主通道                                  |

### 多 Agent 调度


| 问题                    | 应答锚点                                                                                    |
| ----------------------- | ------------------------------------------------------------------------------------------- |
| 子 Agent 上下文怎么传？ | 三档：passthrough / 共享记忆池（按 key 最小授权）/ 增量摘要——最小必要，绝不广播主会话全文 |
| 子任务失败？            | Worker Pool 限流 + 死信队列（可重放可关闭，不阻塞父任务）                                   |
| 防打爆本地算力？        | 并发上限 + 队列排队——端侧不能水平扩容，限流是生存条件                                     |

### 前端与桌面端


| 问题                          | 应答锚点                                                                      |
| ----------------------------- | ----------------------------------------------------------------------------- |
| 流式渲染卡不卡？              | 增量 append + 虚拟化；SSE 事件分通道渲染                                      |
| 状态为什么用 store 不用 ref？ | **踩坑金句**：组件 remount 后 useRef 状态丢失，迁 store 会话级持久化          |
| 打包与启动优化？              | PyInstaller 单独打 +`_MEIPASS` 资源路径回退 + 驱动包离线内置（内网无 pip 源） |

### 测试与工程纪律


| 问题                   | 应答锚点                                                                      |
| ---------------------- | ----------------------------------------------------------------------------- |
| 三语言栈质量保证？     | 三栈分别进 CI：pytest + ruff + mypy strict / vitest / clippy -D warnings      |
| LLM 不确定输出怎么测？ | 脚本化假 LLM 跑端到端状态机 + 黄金用例集卡准确率门槛 + 真模型集成测试单独标记 |
| 协议三处同步靠什么？   | 单一来源 + 专门同步测试 + PR 检查项——人肉纪律必须有机器兜底                 |

### 冷门偷袭题


| 问题                    | 应答锚点                                                                        |
| ----------------------- | ------------------------------------------------------------------------------- |
| checkpointer/会话恢复？ | 状态外置存储 + thread_id 恢复，重点讲"状态可序列化、任意节点续跑"               |
| 上云多租户最大改造点？  | 审计分租户 / HITL 改异步通知 / 凭证换云 KMS / MCP 进程池化                      |
| 最不满意的设计？        | 主动暴露有改进路线图的弱点（如 L2 Gist 占位、路由策略表静态）——比假装完美可信 |

---

## 第 4 章：临场弹药（可直接背）

### 4.1 答题纪律（通用）

1. **三枚踩坑金句随身带**（见 0.2，技术面最打动人的永远是"我真踩过"）；
2. **每题结尾带一句取舍逻辑**："我选 A 不选 B，因为在 C 场景下…"——架构师和码农的分界线；
3. **不会的题兜底句式**："这个我没有生产经验，但基于我在 X 上的经验，我会先验证 A、再考虑 B"——诚实 + 迁移能力比硬编安全；
4. **数字要说得出**：50+ 客户、6 个微服务、30+ 内置工具（八批演进）、4 个 MCP Server——每个数字后面备一个具体例子；注意 1.5× 是验收阈值、90% 是 CI 门槛，口径不要说成"实测均值"；
5. **个人项目防真实性追问口径**：核心链路和架构一人完成，用了成熟开源组件（Tauri/llama.cpp/tree-sitter），价值在编排、策略和工程兜底层；
6. **别贬低 Java 经历**：定位"复合背景"——大型系统架构纪律 + AI 工程化，是纯 AI 背景候选人缺的；
7. **银行/政企客户保密红线**：面试中绝不点客户名，一律说"某股份制银行""某政企客户"——说漏嘴是重大减分项，守口如瓶本身也是职业素养展示；
8. **答题结构**：结论→理由→例子，单题 90 秒内；没听清就复述确认（"您问的是 XX 对吗"）；被打断是好事，立刻跟面试官方向走；
9. **心态切换**：从"被考察"切到"技术交流"——你是带着完整作品来聊的人，不是来答题的人。

---

### 4.2 数字问题实战应答脚本（被问到时直接背）

**1. "50+ 客户、6 个微服务是什么概念？"**

> "这是我们业务中台的部署规模——同一套核心系统服务 50 多家银行和政企客户，每家一套独立部署，共用一套代码基线。这也是为什么架构上必须做服务化和报文协议统一：任何一家客户的定制需求都不能污染主干，否则 50 家没法维护。"

- 深追"具体哪几家？"→ 保密话术："客户名单有保密协议，类型以股份制银行和城商行为主"，随即引到架构拆分逻辑（按变更频率和依赖方向拆 6 服务，流程引擎和调度被依赖最多所以独立）——**把追问从客户名引到你的主场**。

**2. "内置工具具体多少个？"**

> "内置工具 30 多个，算上 Rust 桥接的文件系统工具一共 43 个。数字不是重点，重点是这批工具**按风险等级分八批演进（V0–V8）**：从 5 个文件读写核心，扩到轻量工具、Git 工具、文档处理族（Excel/PDF/Word）、大文件日志查看，最后一批是模型接入管理。每个工具注册必须带三样：JSON Schema、描述、风险等级——风险等级直接驱动 HITL：读操作放行、写操作进闸门、shell 这种 critical 级强制人工审批。"

- 深追"为什么分八批不一次设计全？"→ "一次设计全等于一次把所有风险面打开；分批让审批策略和测试独立验收，和微服务渐进演进一个思路。"

**3. "加速比实测多少？"（必须说成验收标准）**

> "我先说数字口径：我们不拿单点数字汇报，而是建了独立 benchmark 场景集，**双门槛验收——加速比 ≥1.5× 且 token 接受率 ≥60%，两个都达标才算通过**。典型任务落在 1.5–2 倍区间，结构化输出任务收益最高。纯 CPU 环境我不会给您论文里 GPU 那种 3 倍数字，那是不同的硬件前提。"

- 深追"什么情况反而变慢？"→ 直接接四道闸（短输出跳过 / n_draft<2 关闭 / 任务分档 / 敏感硬关）。
- 为什么这么答：把 1.5× 定位成验收标准——① 诚实（代码里确实是验收阈值）；② 展示量化验收体系，比报漂亮实测数更显工程成熟度。

**4. "路由准确率 90% 怎么测的？"（必须说成 CI 门槛）**

> "准确说法是：我们维护了黄金用例集，关键词层路由准确率 **90% 是 CI 硬门槛**，低于这个数测试直接失败进不了主干；混合任务分解门槛是 85%。我不喜欢报'实测 95%'这种没法复现的数字——**门槛写进 CI，任何人随时能验证，这才算数**。"

- 深追"用例集怎么维护？"→ "新发现的边界指令就补进用例集，它本身就是路由器的回归测试。"

**通用收束句（面试官连着追几个数字后用这句升维）：**

> "您问的这几个数字我有一个共同点：**我习惯把指标做成可验证的门槛，而不是汇报用的亮点**——90% 在 CI 里、1.5× 在验收脚本里、工具数在注册表里。数字可以随时被代码验证，这比记住数字本身重要。"
> （面试官记住的不会是数字大小，而是"这个人的数字都经得起查"。）

---

### 4.3 杀手锏：被要求"讲一个亮点"时——内网小模型输出稳定性硬兜底体系（首选，唯一推荐）

**为什么是它：**

1. 精准命中 JD 第 5 条"幻觉、**输出不稳定**"——联想用 NPU 跑 7B，这就是他们每天的头号难题；
2. 强差异化记忆点：90% 候选人答"优化 prompt"，你的答案是反共识的——**"prompt 对小模型是软约束，我不相信说服，只做硬兜底"**；
3. 要素齐全：真实战损 + 三层防御 + 11 条回归测试；
4. 能升维成方法论金句（个人签名）：**"模型可以不可靠，系统必须可靠。"**

**30 秒版**（时间紧/穿插用）：

> "我最想讲的是内网小模型的输出稳定性问题。我们碰到过模型无视中文指令用英文作答、甚至把思维链原文吐给用户。我的解法不是把 prompt 写得更严厉——对小模型那是软约束——而是三层硬兜底：确定性检测、受控模块重写、降级保流程。核心一句话：**模型可以不可靠，系统必须可靠。**"

**90 秒版**（正式问"最有成就感的设计"时用）：

> "现象：部署的推理模型两个毛病——无视语言约束用英文作答；把 `<think>` 内心独白塞进最终回复。
> 我最初的直觉也是改 prompt、加 MANDATORY 字样——没用。踩了几次后想明白：**prompt 对小模型是软约束，指望它自觉是架构上的失职。**
> 于是三层硬兜底：① prompt 纪律打底但只当软约束；② **确定性检测**——字符集统计这类死算法判违规，绝不让模型自我判断；③ **外部干预**——违规草稿送带强中文约束的受控模块重写，失败回退原稿，主流程永不断。配套：剥离 think 标签、分类任务输出收窄到单枚举词。整套机制 11 条回归测试钉住边界。
> 沉淀成方法论：**模型可以不可靠，系统必须可靠——检测用确定性算法，修复用受控模块，失败用降级兜底。**这条原则后来贯穿整个系统：路由解析失败降级到先验、加速组件挂了静默关闭、验证不过禁止声称完成。"

**为什么不是其他候选（防自己摇摆）：**


| 候选                | 不选的原因                                                     |
| ------------------- | -------------------------------------------------------------- |
| DSpark 推测解码     | 数字只是验收阈值，撑不起"最大亮点"故事弧                       |
| MACC 三层压缩       | 结构宏大但 L2 是占位，深追有减分风险                           |
| ModeRouter 三级路由 | 很强但是"常规智能设计"；硬兜底是"反共识洞察"，记忆度高一个量级 |
| SSE 断连修复        | 体量撑不起亮点，适合当追问时的补充弹药                         |

被追"还有别的吗"→ 把 ModeRouter 作为第二亮点补上：两个组合恰好是"智能决策层 + 确定性兜底层"，呼应整场主线。

**追问防御 1："受控模块是什么？"（已与代码核实）**

具体指 `summarise`（汇总）任务——不是独立服务，而是 LLM 路由器里一个**用途单一、输出契约极窄的专用调用**。链路：终答草稿 → `_needs_chinese_rewrite()` 字符集统计判违规 → 命中调 `llm.summarise(draft)` 重写 → 仍无中文/抛异常则回退原稿主流程不断。

"受控"的三层含义（答题内核）：

1. **任务受控**：不回答问题、不规划、不调工具，只干"把给定材料汇总成终答"一件事——任务越窄小模型行为越可预测，这是和"让主模型顺便翻译一下"的本质区别；
2. **提示词受控**：专属提示词文件（summarise.md）带 MANDATORY 级约束——必须中文、时间只能来自注入的 Current time（防编造日期，真实踩坑）、只能用给定材料的事实（防幻觉）、严格 JSON 输出；**同一个模型在不同任务通道里服从不同约束强度**；
3. **结果受控**：重写结果还要再校验一次，仍不含中文就回退原稿——**受控模块也不被无条件信任，兜底的兜底还是降级**。

一句话版：

> "受控模块是一个任务单一、提示词高强度约束、输出契约窄的专用 LLM 调用。核心思想：**同一个模型在窄任务上比在宽任务上听话得多**。我不指望主模型自觉守约束，而是把违规输出交给专门强化过的通道去修，修完再校验，不合格就降级——信任链条上每一环都有下一环兜着。"

升维点（懂行面试官会眼前一亮）：**这本质是微服务时代"单一职责"思想在 LLM 调用层的应用**——再次呼应"企业级工程能力复用到 AI"的转型主线。

**追问防御 2："重写后还是英文，怎么办？"（降级链最后一环，考取舍）**

直接答案（不回避）：

> "**原样输出英文草稿**，一字不改地给用户。代码里写死的降级逻辑：重写失败或结果仍不含中文就回退原稿——**终答永远有产出，流程永远不断**。"

取舍理由（答题灵魂）：

1. **用户首要需求是拿到答案，不是拿到完美答案**——内网环境用户可能根本没有第二个渠道拿到这个答案；
2. **原稿比重写稿更可信**：每次模型改写都有语义漂移风险，重写出"曲解原意的中文"比英文原稿更糟——错误语言的错误信息 > 正确语言的错误信息 > 错误语言的正确信息；
3. **降级链必须有底不能无限递归**："重写失败再重写"会让兜底机制本身变成新故障源。

补偿措施（不是躺平，是闭环）：

1. **留痕**：违规记进日志/审计，它是一份模型质量数据；
2. **反哺路由**：这类数据进入 LLM 路由评估，某模型在语言敏感任务反复违规则降优先级切给指令遵循更好的模型——**一次兜底是止血，统计兜底是治本**；
3. **回归测试**：触发问题的输入补进黄金用例集。

收尾金句：

> "整条降级链是：**中文重写 → 英文原稿 → 极端情况明确告知无法处理——任何一级都有产出，系统永远不会在'追求完美'上卡死。**兜底系统的红线不是'输出必须完美'，而是'输出永远存在'。"

追问要点：

- 为什么不在前端提示"这是英文原稿"？可以做且成本低，但优先级排在"先保证有答案"之后——展示层提示是体验优化，不是正确性问题；
- 用户投诉看不懂怎么办？这已超出单次兜底范围，进入模型选型问题——正是路由层"指令遵循"评估维度的价值：**让系统性问题在路由层解决，而不是在每次请求里硬扛**；
- 换更大的重写模型？重写是高频低成本任务，用强模型做翻译性价比低；更合理的是把中文遵循能力作为模型**接准入测试项**——不达标的不进候选池。

---

## 第 5 章：面试前 24 小时检查清单（按风险从高到低）

### A. 简历一致性自查（最容易被戳穿的地方）


| 检查项                                | 风险                   | 口径准备                                                                                                          |
| ------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 2019.06 入职 vs 中台"2021.06–至今"   | "前两年在干嘛"         | 运营作业基础模块开发，2021 起进入中台架构                                                                         |
| digPref 2026.03–至今只有半年但体量大 | "一个人半年做这么大？" | 核心链路一人 + 复用成熟开源组件（Tauri/llama.cpp/tree-sitter），价值在编排与策略层；且是 7 年企业级经验的快速复用 |
| "转型"与在职状态                      | 全职转型还是在职转型？ | 口径必须与社保/背调对得上，提前统一，不能含糊                                                                     |
| 自评星级（Python/LangGraph 五星）     | 故意深挖五星项         | 五星项全部有代码和测试兜底，保持自信但不加码                                                                      |

### B. JD 暗雷："可验证的用户规模/业务效果"（个人项目没有用户数）

双线准备：

1. **B 端线（真实数据）**：集中作业系统覆盖 20+ 类银行业务场景、端到端线上化——现在就去翻项目资料找 1-2 个真实效率数字（处理时长缩短/人力节省百分比）；
2. **个人项目线（换指标）**：不编用户数，改报工程指标——38 个测试文件、三栈 CI 全绿、BUGFIX 记录 130+、30+ 内置工具（八批演进）+ 4 MCP；再补一句"这是技术验证项目，用户规模要依托平台落地，这也是我加入联想的原因"——**把短板转成求职动机**。

### C. 必须提前准备的四样材料（口述十分钟不如眼见一分钟）

1. **一张架构图**：三层架构 + 两条通信通道，提前画好存着，3 分钟讲完，节点名与真实代码一致（mode_router / hitl_gate / SseBridge）；
2. **1-2 分钟产品录屏**：digPref 跑完整任务（提问→思维链流式→工具执行→审批卡片→结果），面试官要演示时直接放；
3. **GitHub 仓库整洁**：README 清晰、无敏感信息（内网地址/凭证/真实客户名）、最近有提交记录；
4. **数字出处**（口径已与代码核实，简历上的"19 个内置工具"需同步改为"30+"）：见 [4.2 数字脚本](#42-数字问题实战应答脚本被问到时直接背)；核心结论：50+ 客户/6 微服务来自真实经历；30+ 工具（代码实为 43）；1.5× 是验收阈值；90% 是 CI 门槛。

### D. 高风险话术（提前背）

1. **离职原因**：不说前司坏话——"平台成熟稳定，但我想在端侧 AI 的结构性窗口期押注自己的下一个十年"；
2. **薪资**：岗位 20–30K，期望报区间中上（如 26–30）+ "更看重岗位匹配，具体可谈"；提前想好底线数字；
3. **最大缺点**：用已克服的判断力成长作答——"早期习惯把架构做全做重，现在学会按规模配基建（数据量没到十万级不上向量库）"；
4. **学历**：若被问，用"7 年工程产出 + 独立交付复杂系统"对冲，不回避不纠缠。

### E. 面试当天执行清单（逐项打勾）

- [ ]  摄像头平视角度 / 麦克风无回声 / 背景干净 / 有线网络 / 手机静音 / 提前 10 分钟进会议室测试；
- [ ]  架构图、录屏、简历电子版放在手边，共享屏幕不会手忙脚乱；
- [ ]  纸笔在手：复杂问题记下关键词再答；
- [ ]  自我介绍 2 分钟版本背熟（AI 项目放最前）；
- [ ]  反问环节 2 个问题备好（战略层 + 岗位层）；
- [ ]  每个大回答结尾留一个可展开的钩子，把追问引向主场（各题的"追问要点"就是为此准备的）。

### F. 心态定话（进会议室前默读）

> 这套题覆盖了端侧岗位 90% 的技术面可能性，每一条都有真实代码背书。我和大多数候选人的区别是：他们背的是概念，我讲的全是自己写过的代码和踩过的坑。我不是来答题的，我是带着一个完整作品来交流的。
